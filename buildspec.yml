version: 0.2

phases:
  build:
    commands:
      - echo Build started on `date`
      # - mvn test
  post_build:
    commands:
      - echo Build completed on `date`
      - sudo mvn package /com.webapp

artifacts:
  files:
    - '**/*'
