version: 0.2

phases:
  build:
    commands:
      - echo Build started on `date`
      # - mvn test
  post_build:
    commands:
      - echo Build completed on `date`
      - cd path_to_pom.xml
      - mvn package 

artifacts:
  files:
    - '**/*'
